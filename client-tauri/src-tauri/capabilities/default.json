{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Permessi base: finestra principale + eventi core",
  "windows": ["main"],
  "permissions": ["core:default"]
}
